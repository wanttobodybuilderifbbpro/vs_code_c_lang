#include <iostream>
#include <vector>
using namespace std;

int n, m, k;
vector <int> arr;

int main(){
  cin >> k;

  for(int i = 0; i < k; i++){
    int x;
    cin >> x;
    arr.push_back(x);
  }

  //입력하다가 같은게 나오면 break 걸고 그렇지 않으면 continue
  for(int i = 0; i < arr.length; i++){
    if(){

    }
    else break;
  }

  cout << << '\n';
}